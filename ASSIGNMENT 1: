#task 1
# Ask the user to enter two numbers
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

# Perform the operations
addition = num1 + num2
subtraction = num1 - num2
multiplication = num1 * num2
division = num1 / num2  # Note: This will give an error if num2 is 0

# Print the results without decimals if not needed
print("Addition:", int(addition) if addition == int(addition) else addition)
print("Subtraction:", int(subtraction) if subtraction == int(subtraction) else subtraction)
print("Multiplication:", int(multiplication) if multiplication == int(multiplication) else multiplication)
print("Division:", int(division) if division == int(division) else division)




# Task 2 :
# Personalized Greeting Program

# Take user's first and last name as input
first_name = input("Enter your first name: ")
last_name = input("Enter your last name: ")

# Concatenate to form the full name
full_name = first_name + " " + last_name

# Print personalized greeting
print("\nHello,", full_name, "! Welcome to the Python program.")

